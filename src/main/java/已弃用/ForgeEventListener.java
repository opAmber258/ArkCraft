/*
package 已弃用;


import com.MyMod.myMod.ModCommands.GetArknightsExpCommand;
import com.MyMod.myMod.Utils;
import net.minecraftforge.event.RegisterCommandsEvent;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;

@Mod.EventBusSubscriber(modid = Utils.MOD_ID, bus = Mod.EventBusSubscriber.Bus.FORGE)
public class ForgeEventListener {

    @SubscribeEvent
    public static void registerCommand(RegisterCommandsEvent event) {
        GetArknightsExpCommand.register(event.getDispatcher());
    }
}
*/